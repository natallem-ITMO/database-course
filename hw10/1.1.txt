1.1. FreeSeats
start transaction read only isolation level read committed;
-- RU - не может быть, т.к. есть возможность прочитать 
-- информацию о полете, которого на самом деле никогда не будет 
-- (например та транзакция, в которой полет добавлялся, откатится), 
-- и вернуть пользователю свободные места на полет, которого нет.
-- RC - т.к. у нас есть один запрос,неповторяемое чтение и фантомные
-- записи не смогут отразиться на корректности результата, ведь читаем
-- все данные один раз.
