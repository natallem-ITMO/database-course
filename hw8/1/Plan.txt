-- ДЗ-5.3.4 Информацию о студентах с :Mark по предмету :LecturerName
-- ДЗ-5.3.5 Информацию о студентах с заданной оценкой (:Mark) по
-- предмету Который вёл (:LecturerId);
-- ДЗ-5.6.3 StudentId имеющих оценки по всем предметам :LecturerName
-- используется в качестве связующего индекса => должен быть
-- упорядоченным (на дереве)
create index plan_lecturer_id_course_id_index on Plan using btree
    (LecturerId, CourseId)

-- ДЗ-5.6.2 StudentId не имеющих оценок у :LecturerName
-- ДЗ-5.6.1 StudentId имеющих хотя бы одну оценку у :LecturerName
-- ДЗ-5.6.4 StudentId имеющих оценки по всем предметам
-- :LecturerName, которые он у него вёл
-- используется в качестве связующего индекса => должен быть
-- упорядоченным (на дереве)
create index plan_lecturer_id_group_id_index on Plan using btree
    (LecturerId, GroupId)

-- ДЗ-5.5.1 ФИО студента и названия предметов которые у него есть
-- по плану
-- ДЗ-5.5.2 ФИО студента и названия предметов которые у него есть
-- без оценки
-- ДЗ-5.5.3 ФИО студента и названия предметов которые у него есть,
-- но не 4 или 5
-- используется в качестве связующего индекса => должен быть
-- упорядоченным (на дереве)
create index plan_course_id_group_id_index on Plan using btree
    (CourseId, GroupId)

-- ДЗ-5.5.1 ФИО студента и названия предметов которые у него есть по
-- плану
-- ДЗ-5.5.2 ФИО студента и названия предметов которые у него есть без
-- оценки
-- ДЗ-5.5.3 ФИО студента и названия предметов которые у него есть, но
-- не 4 или 5
-- используется в качестве связующего индекса => должен быть
-- упорядоченным (на дереве)
create index plan_group_id_course_id_index on Plan using btree
    (GroupId, CourseId)